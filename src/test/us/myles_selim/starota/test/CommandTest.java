package us.myles_selim.starota.test;

public class CommandTest {

	@TestTarget
	public static boolean test() {
		// MessageChannel channel = Starota.getChannel(567904084095401985L);
		// try {
		// new us.myles_selim.starota.commands.CommandTest().execute(new
		// String[] { ".test" },
		// channel.createMessage(".test"), channel.getGuild(), channel);
		// } catch (Exception e) {
		// e.printStackTrace();
		// return false;
		// }
		return true;
	}

}
